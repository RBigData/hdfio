AC_PREREQ([2.69])
AC_INIT(DESCRIPTION)

AC_PROG_CC_C99

AC_OPENMP

if test -n "${OPENMP_CFLAGS}"; then
  ADDITIONAL_FLAGS=""
else
  ADDITIONAL_FLAGS="\$(SHLIB_OPENMP_CFLAGS)"
fi

AC_SUBST(ADDITIONAL_FLAGS)
AC_OUTPUT(src/Makevars)
